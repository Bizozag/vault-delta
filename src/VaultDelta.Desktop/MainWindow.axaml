<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:VaultDelta.Desktop.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignWidth="1280"
        d:DesignHeight="780"
        x:Class="VaultDelta.Desktop.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Vault Delta"
        Width="1280"
        Height="780"
        MinWidth="1024"
        MinHeight="680"
        WindowStartupLocation="CenterScreen">
    <Grid RowDefinitions="*,34" Background="#F3F1EC">
        <Grid Grid.Row="0" ColumnDefinitions="248,*">
            <Border Grid.Column="0" Background="#172B27" Padding="18,22,18,18">
                <Grid RowDefinitions="Auto,36,*,Auto">
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
                        <Border Width="40" Height="40" CornerRadius="11" Background="#1DAA91">
                            <Path Width="23"
                                  Height="23"
                                  Stretch="Uniform"
                                  Fill="White"
                                  Data="M3,6 L10,6 L10,3 L15,8 L10,13 L10,10 L3,10 Z M21,14 L14,14 L14,11 L9,16 L14,21 L14,18 L21,18 Z" />
                        </Border>
                        <StackPanel VerticalAlignment="Center" Spacing="0">
                            <TextBlock Text="VAULT DELTA" FontSize="15" FontWeight="Bold" LetterSpacing="1.1" Foreground="White" />
                            <TextBlock Text="离线增量工具" FontSize="11" Foreground="#91A7A1" />
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Margin="12,18,0,0" Text="工作区" FontSize="11" FontWeight="SemiBold" LetterSpacing="1.1" Foreground="#708B84" />

                    <StackPanel Grid.Row="2" Margin="0,16,0,0">
                        <Button x:Name="CompareNavigation" Classes="nav" Classes.active="{Binding IsCompareSelected}"
                                Command="{Binding NavigateCommand}" CommandParameter="Compare"
                                AutomationProperties.Name="创建增量补丁">
                            <Grid ColumnDefinitions="28,*">
                                <TextBlock Grid.Column="0" Text="⇄" FontSize="20" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="1">
                                    <TextBlock Text="比较快照" FontWeight="SemiBold" />
                                    <TextBlock Text="创建增量补丁" FontSize="11" Opacity="0.7" />
                                </StackPanel>
                            </Grid>
                        </Button>

                        <Button x:Name="PatchesNavigation" Classes="nav" Classes.active="{Binding IsPatchesSelected}"
                                Command="{Binding NavigateCommand}" CommandParameter="Patches"
                                AutomationProperties.Name="补丁与恢复">
                            <Grid ColumnDefinitions="28,*">
                                <TextBlock Grid.Column="0" Text="▤" FontSize="18" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="1">
                                    <TextBlock Text="补丁与恢复" FontWeight="SemiBold" />
                                    <TextBlock Text="检查 · 应用 · 回滚" FontSize="11" Opacity="0.7" />
                                </StackPanel>
                            </Grid>
                        </Button>

                        <Button x:Name="SettingsNavigation" Classes="nav" Classes.active="{Binding IsSettingsSelected}"
                                Command="{Binding NavigateCommand}" CommandParameter="Settings"
                                AutomationProperties.Name="设置">
                            <Grid ColumnDefinitions="28,*">
                                <TextBlock Grid.Column="0" Text="⚙" FontSize="17" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="1">
                                    <TextBlock Text="设置" FontWeight="SemiBold" />
                                    <TextBlock Text="范围与存储" FontSize="11" Opacity="0.7" />
                                </StackPanel>
                            </Grid>
                        </Button>
                    </StackPanel>

                    <Border Grid.Row="3" Padding="12" CornerRadius="10" Background="#203833" BorderBrush="#315048" BorderThickness="1">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                            <Border Width="8" Height="8" CornerRadius="4" Background="#48D3A9" VerticalAlignment="Top" Margin="0,5,0,0" />
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock Text="本地模式" FontSize="12" FontWeight="SemiBold" Foreground="#E4F1ED" />
                                <TextBlock Text="无网络 · 无遥测" FontSize="11" Foreground="#8FA9A2" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="1" RowDefinitions="92,*">
                <Border Grid.Row="0" Background="#FAF9F6" BorderBrush="#DDDFDA" BorderThickness="0,0,0,1" Padding="34,19,32,17">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Spacing="3">
                            <TextBlock Text="{Binding PageTitle}" FontSize="25" FontWeight="SemiBold" Foreground="#17211F" />
                            <TextBlock Text="{Binding PageDescription}" Classes="muted" FontSize="13" />
                        </StackPanel>
                        <Border Grid.Column="1" VerticalAlignment="Center" Background="#E6F3EF" BorderBrush="#C6E3DA" BorderThickness="1" CornerRadius="18" Padding="12,7">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <Border Width="7" Height="7" CornerRadius="4" Background="#087C6A" VerticalAlignment="Center" />
                                <TextBlock Text="准备就绪" FontSize="12" FontWeight="SemiBold" Foreground="#075F52" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <ScrollViewer x:Name="ComparePage" IsVisible="{Binding IsCompareSelected}" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="34,26,34,42" Spacing="18" DataContext="{Binding Compare}">
                            <Border Classes="card" Padding="22">
                                <StackPanel Spacing="16">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="COMPARE" Classes="eyebrow" />
                                            <TextBlock Text="选择两个独立快照" Classes="sectionTitle" FontSize="20" />
                                            <TextBlock Text="扫描只读取文件。两个目录不能相同，也不能互相包含。" Classes="muted" FontSize="12" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Background="#EAF3F0" CornerRadius="16" Padding="12,6" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding StatusText}" Foreground="#075F52" FontSize="12" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>

                                    <Grid ColumnDefinitions="*,26,*" ColumnSpacing="8">
                                        <Border Grid.Column="0" Padding="14" CornerRadius="10" Background="#F7F8F4" BorderBrush="#DDE0DA" BorderThickness="1">
                                            <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
                                                <TextBlock Text="01  旧快照 / 基线" FontSize="12" FontWeight="SemiBold" Foreground="#53605C" />
                                                <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                                    <TextBox Text="{Binding BaselinePath}" PlaceholderText="选择原始 Obsidian 文件夹" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="11" />
                                                    <Button Grid.Column="1" x:Name="SelectBaselineButton" Content="选择…" Classes="secondary" Command="{Binding SelectBaselineCommand}" />
                                                </Grid>
                                            </Grid>
                                        </Border>
                                        <TextBlock Grid.Column="1" Text="→" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#87928E" />
                                        <Border Grid.Column="2" Padding="14" CornerRadius="10" Background="#F7F8F4" BorderBrush="#DDE0DA" BorderThickness="1">
                                            <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
                                                <TextBlock Text="02  新快照 / 目标" FontSize="12" FontWeight="SemiBold" Foreground="#53605C" />
                                                <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                                    <TextBox Text="{Binding TargetPath}" PlaceholderText="选择更新后的 Obsidian 文件夹" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="11" />
                                                    <Button Grid.Column="1" x:Name="SelectTargetButton" Content="选择…" Classes="secondary" Command="{Binding SelectTargetCommand}" />
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </Grid>

                                    <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                        <TextBlock Text="包含 .obsidian/plugins/** 与 themes/** · 排除 .trash/**" Classes="muted" FontSize="11" VerticalAlignment="Center" />
                                        <Button Grid.Column="1" x:Name="CancelCompareButton" Content="取消" Classes="secondary" Command="{Binding CancelCommand}" IsVisible="{Binding IsBusy}" />
                                        <Button Grid.Column="2" x:Name="StartCompareButton" Content="开始比较" Classes="primary" Command="{Binding CompareCommand}" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border x:Name="ScanningPanel" Classes="card" Padding="22" IsVisible="{Binding IsBusy}">
                                <Grid ColumnDefinitions="*,220" ColumnSpacing="28">
                                    <StackPanel Spacing="10">
                                        <TextBlock Text="{Binding StatusText}" Classes="sectionTitle" />
                                        <ProgressBar IsIndeterminate="True" Height="7" />
                                        <TextBlock Text="{Binding CurrentPath}" Classes="muted" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="11" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                        <TextBlock Text="已处理条目" Classes="muted" FontSize="11" />
                                        <TextBlock Text="{Binding ProcessedEntries}" FontSize="28" FontWeight="SemiBold" Foreground="#087C6A" />
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border x:Name="CompareErrorPanel" Classes="card" Padding="20" IsVisible="{Binding IsError}" BorderBrush="#E7BDA1">
                                <StackPanel Spacing="6">
                                    <TextBlock Text="无法完成比较" FontWeight="SemiBold" Foreground="#934A24" />
                                    <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Classes="muted" />
                                    <TextBlock Text="已保留目录选择，可直接调整后重试。" Classes="muted" FontSize="11" />
                                </StackPanel>
                            </Border>

                            <Border x:Name="CompareEmptyPanel" Classes="card" Padding="24" IsVisible="{Binding IsEmptyState}">
                                <Grid ColumnDefinitions="*,240" ColumnSpacing="28">
                                    <StackPanel Spacing="7">
                                        <TextBlock Text="差异审核区" Classes="sectionTitle" />
                                        <TextBlock Text="比较完成后，这里会列出新增、修改、删除和重命名。筛选只改变显示，不会改变底层差异结果。" Classes="muted" TextWrapping="Wrap" LineHeight="20" />
                                        <TextBlock Text="生成补丁将在审核完成后的下一阶段开放。" Foreground="#087C6A" FontSize="12" FontWeight="SemiBold" />
                                    </StackPanel>
                                    <Border Grid.Column="1" CornerRadius="10" Background="#172D28" Padding="16">
                                        <StackPanel Spacing="8">
                                            <TextBlock Text="风险项会自动标记" Foreground="#E1EFEB" FontWeight="SemiBold" />
                                            <TextBlock Text="删除 · 重命名&#x0a;Obsidian 配置&#x0a;同路径类型变化" Foreground="#94AAA4" FontSize="12" LineHeight="21" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <StackPanel x:Name="CompareResultPanel" Spacing="14" IsVisible="{Binding HasResult}">
                                <Grid ColumnDefinitions="*,*,*,*,1.35*,*" ColumnSpacing="10">
                                    <Border Grid.Column="0" Classes="metric"><StackPanel><TextBlock Text="{Binding AddedCount}" FontSize="22" FontWeight="SemiBold" Foreground="#087C6A"/><TextBlock Text="新增" Classes="muted" FontSize="11"/></StackPanel></Border>
                                    <Border Grid.Column="1" Classes="metric"><StackPanel><TextBlock Text="{Binding ModifiedCount}" FontSize="22" FontWeight="SemiBold" Foreground="#356A8A"/><TextBlock Text="修改" Classes="muted" FontSize="11"/></StackPanel></Border>
                                    <Border Grid.Column="2" Classes="metric"><StackPanel><TextBlock Text="{Binding DeletedCount}" FontSize="22" FontWeight="SemiBold" Foreground="#A45C23"/><TextBlock Text="删除" Classes="muted" FontSize="11"/></StackPanel></Border>
                                    <Border Grid.Column="3" Classes="metric"><StackPanel><TextBlock Text="{Binding RenamedCount}" FontSize="22" FontWeight="SemiBold" Foreground="#6A568F"/><TextBlock Text="重命名" Classes="muted" FontSize="11"/></StackPanel></Border>
                                    <Border Grid.Column="4" Classes="metric"><StackPanel><TextBlock Text="{Binding TransferSizeText}" FontSize="15" FontWeight="SemiBold" Foreground="#17211F"/><TextBlock Text="预计传输" Classes="muted" FontSize="11"/></StackPanel></Border>
                                    <Border Grid.Column="5" Classes="metric"><StackPanel><TextBlock Text="{Binding RiskCount}" FontSize="22" FontWeight="SemiBold" Foreground="#9A4D32"/><TextBlock Text="风险项" Classes="muted" FontSize="11"/></StackPanel></Border>
                                </Grid>

                                <Border Classes="card" Padding="18">
                                    <StackPanel Spacing="13">
                                        <Grid ColumnDefinitions="*,Auto">
                                            <StackPanel Spacing="3">
                                                <TextBlock Text="审核变化" Classes="sectionTitle" />
                                                <TextBlock Text="仅显示变化项；未变化条目仍保留在完整比较结果中。" Classes="muted" FontSize="11" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                                <Button Content="全部" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="All" />
                                                <Button Content="新增" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="Added" />
                                                <Button Content="修改" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="Modified" />
                                                <Button Content="删除" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="Deleted" />
                                                <Button Content="重命名" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="Renamed" />
                                                <Button Content="风险项" Classes="filter" Command="{Binding SetFilterCommand}" CommandParameter="Risks" />
                                            </StackPanel>
                                        </Grid>

                                        <Border IsVisible="{Binding HasNoChanges}" Padding="24" CornerRadius="10" Background="#EDF6F2">
                                            <TextBlock Text="两个快照内容一致，无需生成补丁。" HorizontalAlignment="Center" Foreground="#075F52" FontWeight="SemiBold" />
                                        </Border>

                                        <ItemsControl ItemsSource="{Binding FilteredEntries}" IsVisible="{Binding HasChanges}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="vm:DiffReviewItemViewModel">
                                                    <Border Padding="12,10" Margin="0,0,0,7" CornerRadius="8" Background="#F8F8F5" BorderBrush="#E2E3DE" BorderThickness="1">
                                                        <Grid ColumnDefinitions="82,*,Auto" ColumnSpacing="12">
                                                            <Border Background="#E7F2EE" CornerRadius="12" Padding="9,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding TypeText}" FontSize="11" FontWeight="SemiBold" Foreground="#075F52" />
                                                            </Border>
                                                            <StackPanel Grid.Column="1" Spacing="2">
                                                                <TextBlock Text="{Binding Path}" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="12" TextTrimming="CharacterEllipsis" />
                                                                <TextBlock Text="{Binding Detail}" IsVisible="{Binding HasDetail}" Classes="muted" FontSize="11" TextTrimming="CharacterEllipsis" />
                                                                <TextBlock Text="{Binding RiskText}" IsVisible="{Binding HasRisk}" Foreground="#A45C23" FontSize="11" />
                                                            </StackPanel>
                                                            <TextBlock Grid.Column="2" Text="{Binding SizeText}" Classes="muted" FontSize="11" VerticalAlignment="Center" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <Grid ColumnDefinitions="*,Auto">
                                            <TextBlock Text="下一步将把审核结果交给补丁生成 Gate。" Classes="muted" FontSize="11" VerticalAlignment="Center" />
                                            <Button Grid.Column="1" Content="生成补丁（Task 20）" Classes="primary" IsEnabled="False" ToolTip.Tip="补丁生成将在下一阶段接入" />
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="PatchesPage" IsVisible="{Binding IsPatchesSelected}" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="34,30,34,42" Spacing="20">
                            <Grid ColumnDefinitions="*,*" ColumnSpacing="18">
                                <Border Grid.Column="0" Classes="card" Padding="25" MinHeight="238">
                                    <StackPanel Spacing="15">
                                        <Border Width="46" Height="46" HorizontalAlignment="Left" CornerRadius="12" Background="#E2F3EE">
                                            <TextBlock Text="↧" FontSize="25" Foreground="#087C6A" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <StackPanel Spacing="5">
                                            <TextBlock Text="检查并应用补丁" Classes="sectionTitle" />
                                            <TextBlock Text="打开 .zip 或补丁目录，先验证清单、载荷和目标基线，再决定是否更新。"
                                                       Classes="muted" TextWrapping="Wrap" LineHeight="20" />
                                        </StackPanel>
                                        <Button Content="打开补丁" Classes="primary" HorizontalAlignment="Left" IsEnabled="False" />
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="1" Classes="card" Padding="25" MinHeight="238">
                                    <StackPanel Spacing="15">
                                        <Border Width="46" Height="46" HorizontalAlignment="Left" CornerRadius="12" Background="#F9ECDF">
                                            <TextBlock Text="↶" FontSize="25" Foreground="#A45C23" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <StackPanel Spacing="5">
                                            <TextBlock Text="恢复未完成事务" Classes="sectionTitle" />
                                            <TextBlock Text="从 Journal 和备份目录继续回滚。恢复不依赖原补丁包，并会逐项验证。"
                                                       Classes="muted" TextWrapping="Wrap" LineHeight="20" />
                                        </StackPanel>
                                        <Button Content="查找恢复记录" Classes="secondary" HorizontalAlignment="Left" IsEnabled="False" />
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border Classes="card" Padding="24">
                                <Grid RowDefinitions="Auto,Auto" RowSpacing="20">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="最近事务" Classes="sectionTitle" />
                                            <TextBlock Text="此设备尚未产生应用或回滚记录。" Classes="muted" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="0 条记录" Classes="muted" VerticalAlignment="Center" />
                                    </Grid>
                                    <Border Grid.Row="1" Padding="28" CornerRadius="10" Background="#F6F7F3" BorderBrush="#E0E2DC" BorderThickness="1">
                                        <StackPanel HorizontalAlignment="Center" Spacing="6">
                                            <TextBlock Text="暂无事务" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                            <TextBlock Text="应用过的补丁和可恢复事务会显示在这里。" Classes="muted" FontSize="12" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="SettingsPage" IsVisible="{Binding IsSettingsSelected}" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="34,30,34,42" Spacing="18" MaxWidth="920" HorizontalAlignment="Left">
                            <Border Classes="card" Padding="24" Width="900">
                                <StackPanel Spacing="18">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="默认受管理范围" Classes="sectionTitle" />
                                        <TextBlock Text="这些默认值会用于新的比较会话，正式生成前仍会展示实际生效规则。" Classes="muted" />
                                    </StackPanel>
                                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,1,Auto" RowSpacing="14">
                                        <StackPanel Grid.Row="0" Spacing="3">
                                            <TextBlock Text="包含 Obsidian 插件与主题" FontWeight="SemiBold" />
                                            <TextBlock Text=".obsidian/plugins/** · .obsidian/themes/**" Classes="muted" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="11" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Row="0" Grid.Column="1" IsChecked="True" IsEnabled="False" OnContent="包含" OffContent="排除" />
                                        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#E4E5E0" />
                                        <StackPanel Grid.Row="2" Spacing="3">
                                            <TextBlock Text="排除 Obsidian 回收站" FontWeight="SemiBold" />
                                            <TextBlock Text=".trash/**" Classes="muted" FontFamily="Cascadia Mono, Menlo, monospace" FontSize="11" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Row="2" Grid.Column="1" IsChecked="True" IsEnabled="False" OnContent="排除" OffContent="包含" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Classes="card" Padding="24" Width="900">
                                <StackPanel Spacing="18">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="补丁与事务" Classes="sectionTitle" />
                                        <TextBlock Text="MVP 使用安全的跨平台默认值。高级选项将在实际工作流接入后开放。" Classes="muted" />
                                    </StackPanel>
                                    <Grid ColumnDefinitions="*,220" RowDefinitions="Auto,1,Auto" RowSpacing="14">
                                        <StackPanel Grid.Row="0" Spacing="3">
                                            <TextBlock Text="默认补丁格式" FontWeight="SemiBold" />
                                            <TextBlock Text="ZIP 便于跨设备传输；目录形式保留用于检查和调试。" Classes="muted" FontSize="12" />
                                        </StackPanel>
                                        <ComboBox Grid.Row="0" Grid.Column="1" SelectedIndex="0" IsEnabled="False">
                                            <ComboBoxItem Content="ZIP（推荐）" />
                                            <ComboBoxItem Content="目录" />
                                        </ComboBox>
                                        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#E4E5E0" />
                                        <StackPanel Grid.Row="2" Spacing="3">
                                            <TextBlock Text="命令行应用模式" FontWeight="SemiBold" />
                                            <TextBlock Text="MVP 不提供公开 CLI；核心工作流保持 UI 无关。" Classes="muted" FontSize="12" />
                                        </StackPanel>
                                        <Border Grid.Row="2" Grid.Column="1" Background="#F1F2EE" CornerRadius="8" Padding="12,8">
                                            <TextBlock Text="未启用" HorizontalAlignment="Center" Foreground="#68736F" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="Vault Delta 0.1 · .NET 10 · Avalonia 12 · Windows / macOS"
                                       Classes="muted" FontSize="11" Margin="4,2,0,0" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>

        <Border Grid.Row="1" Background="#E9E8E3" BorderBrush="#D5D6D1" BorderThickness="0,1,0,0" Padding="16,0">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
                    <Border Width="6" Height="6" CornerRadius="3" Background="#36A886" />
                    <TextBlock Text="未开始事务" FontSize="11" Foreground="#58635F" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="所有操作均在本机完成" FontSize="11" Foreground="#7A837F" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="v0.1.0" FontSize="11" Foreground="#7A837F" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Window>
